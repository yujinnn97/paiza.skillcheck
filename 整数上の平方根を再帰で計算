def intSquareRootHelper(n,guess):
    if guess**2<=n<(guess+1)**2:
        return guess
    return intSquareRootHelper(n,guess-1)
def intSquareRoot(n):
    # 関数を完成させてください
    return intSquareRootHelper(n,n)

    


